# `polza:qwen3.5-9b` (Qwen3.5 9B, ~6 ГБ Q4) — «читает и не пишет»

- **Задача:** фикстура `parcel-price` / `oversize`, этап 5 со снимка `oversize-plan`.
- **Серия:** r2, 2026-08-30, через polza.

## Что делалось по этапам

- **Этап 5.** Проба 3/3 за секунды — это ОПРОВЕРГЛО локальные вердикты этой же модели
  (0 вызовов и у ollama, и у LM Studio): порог «позвать инструмент» был порогом СРЕДЫ,
  не модели. Но на этапе — 14 вызовов за 1м23с и дерево не изменилось ни на строку:
  4/6 precision (тривиальные), 0/3 human.

## Результат и вердикт

- **Этап 5: ❌ подтверждён на здоровой среде.** Главная ценность записи — правило «среда —
  часть настройки»: планировать роль по имени модели нельзя. Кандидат на перемер этапов
  1–4 с `formFill`.

## Ресурсы

- 1м22с, ₽0.3, 139k/4k токенов, 14 вызовов.

## Файлы

- `reports/` — отчёт r2.
