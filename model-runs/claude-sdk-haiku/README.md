# `claude-sdk:haiku` — контроль дешёвого класса на этапе 5

- **Задача:** фикстура `parcel-price` / `oversize`, этап 5 со снимка `oversize-plan` —
  тот же вход, что у всего класса ≤8 ГБ.
- **Серия:** r8, 2026-08-30, флоу `sdk` (подписка).

## Что делалось по этапам

- **Этап 5.** Код: 4/6 precision + 0/3 human за 2м35с — и провал ровно на том же пороге,
  что у локальных: журнал chunk'а не заполнен, этап красный по стражу. Дозаполнение по
  полям на флоу `sdk` намеренно не распространяется (SDK сам крутит цикл).

## Результат и вердикт

- **Ключевой вывод серии: порог оформления журнала — общий, а не «болезнь локальных».**
  Контроль упал на нём же. (На этапах 1–4 haiku — опорная ✅-модель, см. журнал раннера.)

## Ресурсы

- 2м35с, $0.26, ~8k выходных токенов.

## Файлы

- `reports/` — отчёт r8.
