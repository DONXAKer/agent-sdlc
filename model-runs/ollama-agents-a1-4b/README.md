# `ollama:agents-a1-4b` — верная разведка, зацикливание на уточнениях

**Задача:** та же; вход этапа 5 — снимок эталона; локальная машина (8 ГБ VRAM), ollama,
community-тег `carstenuhlig/omnicoder-9b`-класса — плотная 4B, дообученная на
tool-calling (`InternScience/Agents-A1-4B`), тег `num_ctx 16384`.

## По этапам

| Этап | Что именно сделано | Результат | Артефакт |
|---|---|---|---|
| 1–4 | со снимка эталона | — | из снимка |
| 5 chunk (первый прогон, голый тег) | контекст 4096 — этап сгорел на промпте | ❌ **невалидно**, отозвано: 0 вызовов, инфраструктурный дефект конфига |
| 5 chunk (тег `-ctx16k`) | 13 вызовов, `Task`-разведка верно определила нужные правки | ❌ зациклилась на `AskHuman` (3 круга подряд вместо записи), лимит ходов исчерпан, дерево не изменилось |
| 5 chunk (повтор, после трёх правок раннера) | 20 вызовов, 4м28с | ❌ тот же паттерн — новым проверкам (импорт, числа плана) не за что было зацепиться, кода нет |

## Сравнение с эталоном

| Показатель | `claude-sdk:sonnet` | `agents-a1-4b` |
|---|---|---|
| Скрытые тесты | 9/9 | 4/6 + 0/3 |
| Разведка | ✅ | ✅ **верно** определила точки правки — сильная сторона |
| Закрытие этапа 5 | ✅ сам | ❌ ни строки кода — застряла на уточнениях |

## Итог

**Вердикт: ❌ этап 5.** Дообучение на tool-calling-трассы даёт верную разведку задачи, но
не решает проблему избыточных уточнений: три круга `AskHuman` подряд без перехода к записи.
Устойчиво воспроизведено дважды на одном входе.
