# Единый список кэш-мусора — один источник для install.sh (сверка -x и зачистка установки)
# и test/run-e2e.sh (чистая рабочая копия fixture). Правится только здесь: рукописные копии
# списка в скриптах уже расходились.
# Каталоги чистятся целиком (prune + rm -rf), файлы — по глобу имени.
JUNK_DIRS=(__pycache__)
JUNK_FILES=(.DS_Store '*.pyc')
