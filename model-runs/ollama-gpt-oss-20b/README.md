# `ollama:gpt-oss-20b` — понимает задачу, не пишет код (локальная версия)

**Задача:** та же; вход этапа 5 — снимок эталона; локальная машина (8 ГБ VRAM), ollama,
MoE ~3.6B активных из 20B, MXFP4, тег `num_ctx 16384`. Локальный аналог уже измеренного
облачного `polza:gpt-oss-20b` (см. соответствующую карточку) — закрывает пробел «облачная
версия обкатана, локальной нет».

## По этапам

| Этап | Что именно сделано | Результат | Артефакт |
|---|---|---|---|
| 1–4 | со снимка эталона | — | из снимка |
| 5 chunk | 5 вызовов, 14м09с | ❌ верно пересказала план в тексте ответа, не написала ни строки кода |
| 5 chunk (повтор, после правок раннера) | 16 вызовов, 11м26с | ❌ тот же исход; техническая пометка «⚠️ опасна» без реального ущерба дереву (вероятно, засчитан нераспознанный вызов инструмента, не настоящий отказ политики) |

## Сравнение с эталоном

| Показатель | `claude-sdk:sonnet` | `gpt-oss-20b` (локально) | `polza:gpt-oss-20b` (облако, для сравнения) |
|---|---|---|---|
| Скрытые тесты | 9/9 | 4/6 + 0/3 | 4/6 + 0/3 — то же самое |
| Закрытие этапа 5 | ✅ сам | ❌ бланк | ❌ бланк |

## Итог

**Вердикт: ❌ этап 5, воспроизведено дважды.** Совпадает с уже измеренным поведением
облачной версии — среда (локально/через агрегатор) в этот раз не была искажающим
фактором, в отличие от других моделей в этом же файле.
