# `claude-sdk:sonnet` — эталон (контроль)

**Задача:** фикстура `parcel-price` / `oversize` — надбавка за негабарит в `priceFor`
(40% за сторону >120 см / 90% за сумму >300 см — ставка из ответа человека, правила не
складываются), правило отдельным модулем `src/oversize.ts`.

**Что может:** весь виток, этапы 1–5, без ошибок содержания — по этому прогону сверяются
остальные модели.

**Что не может / ошибки:** этап 6 сорван багом апстрима `claude-agent-sdk` #376 (канал
одобрений рвётся) — не моделью. Воспроизводимая привычка (2/2): не делает `git add` новым
файлам — scope-гейты краснеют по делу.

**Метрики:** скрытые тесты **9/9** — обе границы точные (120 не негабарит / 121 — 40%; 300 не негабарит), скидка от базы, и все три human-кейса: ставка 90% из ответа человека, её пересчёт под другую базу, правило «не складывать» (H1–H3); $3.57 и ~10–15 мин на виток до
verify; ходы по этапам 26/12/—/15/38; отказов политики и разрушений — 0.

**Вердикт:** ✅ эталон. **Файлы:** `artifacts/` — заполненные артефакты этапов 1–4.
