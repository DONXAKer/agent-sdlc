# `polza:gpt-oss-20b` (OpenAI gpt-oss 20B, ~12 ГБ) — дешёвый «оформитель», не рецензент

- **Задача:** фикстура `parcel-price` / `oversize`; этап 5 со снимка `oversize-plan` (r2),
  этап 6 со снимка `oversize-chunk` (r9/r9b). 2026-08-30, через polza.

## Что делалось по этапам

- **Этап 5 (r2).** 9 вызовов за 3м52с — журнал остался бланком, страж красный; 4/6
  precision (тривиальные), 0/3 human. Разрушений нет. ₽0.5.
- **Этап 6 (r9).** Отчёт приёмки закрыла сама и за копейки (₽0.48) — но все гейты в
  отчёте пометила «⏭ не запускался»: **4 расхождения с фактическим прогоном**. Ложного
  зелёного не дала (страховка «худший из двух» выправляет), ревью по существу — ноль
  находок.

## Результат и вердикт

- **Этап 5: ❌. Этап 6: «оформитель», не рецензент.** После этой серии в раннер внесено
  правило: таблицу гейтов отчёта приёмки заполняет рантайм фактами прогона — расхождений
  «отчёт/факт» у оформителя больше не бывает по построению; перемер предстоит.

## Ресурсы

- Этап 5: 3м51с, ₽0.5, 156k/2k токенов. Этап 6: ₽0.48.

## Файлы

- `reports/` — отчёты r2, r9, r9b.
