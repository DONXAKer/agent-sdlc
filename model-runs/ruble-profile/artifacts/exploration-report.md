# Отчёт разведки: Надбавка за негабаритные посылки

> Этап 2. Заполняет агент, проверяет человек **до** плана. Цель: «вот что сейчас в коде, вот где и что менять» — до единой правки. `SDLC.md` → этап 2.

- **Задача:** `intent.md` — Надбавка за негабаритные посылки: добавление поля `surcharge` в объект `Quote` для негабаритных отправлений и расчет надбавки.
- **Гейт «Заполненность артефактов»:** ✅ — все секции заполнены.

## Стек и конвенции

- Язык / стек: TypeScript
- Сборка / тесты: `node scripts/build-check.mjs`, `node --test "test/**/*.test.ts"
- Конвенции: Все изменения должны быть точечными, без нарушения существующей логики и структуры кода.
- Требования к окружению прогона: Ничего особенного, все зависимости установлены через npm.

## Карта кодовой базы

| Файл | Что там сейчас | Что меняем |
|---|---|---|
| src/tariffs.ts | Логика расчета тарифов, функции `priceFor`, `basePrice`, `longestSide`, `dimensionSum` | Добавить логику для проверки негабаритности и расчета надбавки `surcharge` в функции `priceFor` |
| src/ | Отсутствует модуль для проверки негабаритности | Создать новый модуль для логики негабаритности

## Найдено для переиспользования

| Символ | Где (`путь:символ`) | Что делает | Как используем |
|---|---|---|---|
| longestSide | src/tariffs.ts:longestSide | Возвращает наибольшую сторону отправления | Использовать для проверки негабаритности по наибольшей стороне
| dimensionSum | src/tariffs.ts:dimensionSum | Возвращает сумму трёх измерений отправления | Использовать для проверки негабаритности по сумме измерений

_Ничего подходящего не найдено:_ да

## Приёмочный лист, выведенный независимо

| # | Выведенное утверждение | Есть у автора |
|---|---|---|
| 1 | `priceFor` возвращает `Quote` с полем `surcharge` для негабаритных отправлений | да
| 2 | `surcharge` отсутствует в `Quote` для отправлений в пределах габаритов | да
| 3 | Поле `total` учитывает надбавку `surcharge` для негабаритных отправлений | да
| 4 | Существующие тесты остаются зелёными без изменений | да
| 5 | Правило негабаритности вынесено в отдельный модуль | да
| 6 | Надбавка рассчитывается как 40% от базовой цены зоны | да
| 7 | Ставка для суммы измерений > 300 см отличается от 40% | да

**Расхождение:** Списки совпали

**Решение человека о полноте:** Бенчмарк · 2026-08-30

## Точка правки

- src/tariffs.ts:priceFor — Здесь реализуется основная логика расчета цены, и именно здесь нужно добавить проверку негабаритности и расчет надбавки.

## Границы разведки

- Логика взаимодействия с внешними системами не рассматривалась, так как это выходит за рамки текущей задачи.
- Детали реализации для других типов отправлений, кроме негабаритных, не входили в задачи разведки.

## Риски

- Возможные ошибки в логике определения негабаритности могут привести к некорректному расчету надбавки.
- Нарушение существующих тестов при добавлении новой логики.
- Неправильное применение условий для расчета надбавки.

## Всплывшие вопросы

- Нет вопросов.