# `ollama:qwen2.5-coder:7b` — «coder» ≠ агент

**Задача:** фикстура `parcel-price` / `oversize`, этап 5 со снимка; локально (8 ГБ VRAM).

**Что может:** читать (8 вызовов — все чтение); честно не сочиняет несделанное.

**Что не может / ошибки:** ни одной записи — diff пуст, журнал бланком; на другой задаче
та же модель — 0 вызовов за 892 с.

**Метрики:** 4/6 + 0/3 — зелёные только R1/R2 и Pr1/Pr3, проходящие БЕЗ правки; красные Pr2 (надбавка 40%), Pr4 (скидка от базы) и все human (ставка 90%, пересчёт, «не складывать»); 2м41с; бесплатно; 121 926 входных токенов.

**Вердикт:** **❌ этап 5.** «Кодовая» специализация — про текст кода, не про действия.
