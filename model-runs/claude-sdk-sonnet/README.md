# `claude-sdk:sonnet` — контрольный прогон (эталон)

- **Задача:** bench-фикстура `parcel-price`, задача `oversize` — добавить в `priceFor`
  надбавку за негабарит (`Quote.surcharge`): наибольшая сторона >120 см → 40%, сумма трёх
  измерений >300 см → 90% (ставка — из ответа человека на этапе вопросов), правила не
  складываются — применяется бо́льшая; правило вынести отдельным модулем `src/oversize.ts`.
- **Режим:** `--all` (все этапы), флоу `sdk` (подписка Max), 2026-08-29. Роль — контроль:
  по нему сверяются все локальные/дешёвые кандидаты.

## Что делалось по этапам

| Этап | Ходов | Итог |
|---|---|---|
| 1 intent | 26 | `intent.md` + `readiness.md` заполнены, ветка витка проставлена |
| 2 explore | 12 | `exploration-report.md`: два независимых субагента (разведчик + слепой приёмочный лист) |
| 3 ask | — | `clarification-report.md`: оба блокирующих вопроса закрыты ответами человека (ставка 90%, зона far — на общих основаниях) |
| 4 plan | 15 | `plan.md` с files_to_touch, одобрение через автоответчик |
| 5 chunk | 38 | код написан: `src/oversize.ts` + правка `tariffs.ts` + тесты; **все 9 скрытых тестов зелёные**, включая 3/3 human |
| 6 verify | ✗ | opus проанализировал diff, но 4× не смог записать отчёт — баг апстрима `claude-agent-sdk` #376, не модели |

## Результат и вердикт

- Этапы 1–4 ✅, этап 5 ✅ (9/9 щупов), этап 6 — сорван багом SDK, не моделью.
- Воспроизводимая слабость: дважды не сделала `git add` новым файлам — scope-гейты
  покраснели по делу (после этого в раннере появился авто-stage файлов плана).

## Ресурсы

- Деньги: **$3.57** за intent+explore+plan+chunk (без verify).
- Время: ~10–15 мин на виток до verify (по этапам — в `reports/`).

## Файлы

- `artifacts/` — заполненные артефакты этапов 1–4 этого прогона (из снимка `oversize-plan`):
  `intent.md`, `readiness.md`, `exploration-report.md`, `clarification-report.md`, `plan.md`.
- `reports/` — машинные отчёты bench (`oversize` и контроль `freeship`).
