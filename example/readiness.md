# Готовность задачи: PAY-412 — идемпотентность создания платежа

> Заполненный пример по [`../templates/readiness.template.md`](../templates/readiness.template.md).
> Живёт отдельно от задачи: чек-лист внутри проверяемого документа — галочки, которые автор ставит
> сам себе. На этот файл ссылается перенос статуса гейта «Готовность задачи» в
> [`verification-report-1-attempt-1.md`](verification-report-1-attempt-1.md).

## Прогон 1 — перед разведкой

- **Дата:** 2026-07-15

| # | Проверка | Статус | Где видно / что чинить |
|---|---|---|---|
| 1 | Набор гейтов собран, минимум включён | ✅ | `.sdlc/gates.md`, пять обязательных включены |
| 2 | «Что делаем» и «Чего не делаем» заполнены | ✅ | — |
| 3 | Пунктов ≥ 3, из них ≥ 2 с `[edge]` (полный контур) | ✅ | 10 пунктов, 6 с `[edge]` (claim-11, седьмой `[edge]`, добавится после сверки листов) |
| 4 | У каждого пункта заполнено «Как проверить» | ✅ | — |
| 5 | Инварианты названы | ✅ | 3 инварианта, у каждого исполняемая проверка |
| 6 | Блокирующих вопросов без ответа нет | ❌ → ✅ | два блокирующих закрыты ответами человека до входа в виток |
| 7 | Плейсхолдеров `‹…›` не осталось | ✅ | `grep -c '‹' intent.md` = 0 |

**Человеческая часть** — ответы человека, не агента:

- Необходимое: «да — три строки „делаем“ и есть „чего не делаем“ про соседей» · Единичное:
  «да — гонку разбил на 8/9/10» · Однозначное: «да после уточнения кода повтора: 200, не 201» ·
  Проверяемое: «да — у каждого пункта тест и критерий»

**Примеры пересчитаны:** литеральных значений два — коды `200`/`201`/`409` и `count(payments)`;
коды сверены с маппингом `GlobalExceptionHandler` и контрактом guard'а чтением кода, счёт строк —
арифметика в пунктах. Неисполнимых примеров нет.

**Вердикт прогона 1:** готова

## Прогон 2 — перед планом

_Только то, что могло измениться после прогона 1._

- **Дата:** 2026-07-16

| # | Проверка | Статус | Где видно / что чинить |
|---|---|---|---|
| 1 | Блокирующие вопросы закрыты | ✅ | оба `[x]` в «Открытых вопросах», ответы в `clarification-report.md` |
| 2 | Плейсхолдеров не появилось | ✅ | `grep -c '‹' intent.md` = 0 |
| 3 | «Что придётся тронуть» заполнена разведкой | ✅ | 4 пути, проверены человеком |
| 4 | Примеры в изменённых/добавленных пунктах пересчитаны | ✅ | добавлен claim-11 — литерал «ровно одно событие», пересчитывать нечем до теста; помечен в пункте |

**Вердикт прогона 2:** готова
