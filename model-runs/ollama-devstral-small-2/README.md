# `ollama:devstral-small-2` — лучшие опубликованные бенчмарки, ноль строк кода

**Задача:** та же; вход этапа 5 — снимок эталона; локальная машина (8 ГБ VRAM), ollama,
официальный тег `devstral-small-2` (Mistral, 24B, единственная модель серии с
опубликованным SWE-bench Verified 68.0%), тег `num_ctx 16384`.

## По этапам

| Этап | Что именно сделано | Результат | Артефакт |
|---|---|---|---|
| 1–4 | со снимка эталона | — | из снимка |
| 5 chunk (первая попытка) | 6 вызовов, правки журнала, `AskHuman`, одна неудачная `Edit` | ❌ **невалидно**, отозвано — оборвана сетевым таймаутом 600с из-за параллельной докачки другой модели на том же сервере Ollama |
| 5 chunk (чистый прогон, изоляция) | 17 вызовов, весь `stage-timeout` (30 мин) | ❌ **ни строки кода** — всё время ушло на журнал/разведку/`Bash`-проверки |
| 5 chunk (повтор, после трёх правок раннера) | тот же паттерн | ❌ идентично первому чистому прогону |

## Сравнение с эталоном

| Показатель | `claude-sdk:sonnet` | `devstral-small-2` |
|---|---|---|
| Опубликованный SWE-bench Verified | — | **68.0%** (лучший в серии по внешним данным) |
| Скрытые тесты (эта задача) | 9/9 | 4/6 + 0/3 (тривиальные) |
| Закрытие этапа 5 | ✅ сам | ❌ ни строки кода |

## Итог

**Вердикт: ❌ этап 5, воспроизведено дважды.** Резкое расхождение с внешними бенчмарками —
довод в пользу того, что порог методологии («дойти до `Write`/`Edit` реального кода в этом
конкретном цикле tool-use») не совпадает с порогом, который меряет SWE-bench. Не пишет код
не потому, что не умеет решать такие задачи вообще, а потому, что в этой конкретной обвязке
не доходит до записи.
