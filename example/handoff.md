# Передача контекста: PAY-412 — идемпотентность создания платежа

> Заполненный пример по [`../templates/handoff.template.md`](../templates/handoff.template.md).
> Виток **не принят** — передача написана на обрыве, а не на коммите. Так тоже бывает, и именно
> тогда она нужнее всего.

- **Задача:** PAY-412 — идемпотентность создания платежа
- **Предыдущая передача по этой задаче:** нет — это первый виток
- **Приёмка:** не принималась — обрыв: `passed=false`, `action=retry`, виток продолжается
  следующей попыткой chunk'а 1

## Состояние (машиночитаемое)

```yaml
slug: PAY-412
repo: acme-payments
branch: sdlc/PAY-412
base_sha: 9c41f2a
commit: н/п — коммита не было
gates_date: 2026-07-14
chunk: 1
attempts: 1
verdict: failed
published: нет
```

## Гейт «Проверка предусловий публикации»

- Статус: ⏭ — гейт в долге (риск принят, см. «Долг» в [`gates.md`](gates.md)); до коммита виток
  не дошёл, проверять было нечего

## Что сделано

- Обёрнут `PaymentService.create` в существующий `IdempotencyGuard` — свой механизм не писали,
  переиспользовали тот, что работает для заказов (claim-1…claim-3, claim-8…claim-11 в реализации; сверка идёт по полным id)
- Контроллер принимает `Idempotency-Key` и выбирает код ответа по `isReplay()` (claim-1, claim-4, claim-9)
- `PaymentRetryScheduler` обновлён под новую сигнатуру, поведение не менялось
- `PaymentIdempotencyIT` — 9 тестов на 11 пунктов приёмки. **Ни один не прогонялся:** Docker
  в окружении недоступен, Testcontainers не поднялся

## С чего начинать дальше

_Дословно `retry_instruction` из [`verification-report-1-attempt-1.md`](verification-report-1-attempt-1.md):_

- Перенести `limitValidator.check` внутрь `Supplier`, передаваемого в `idempotencyGuard.execute` —
  это подтверждённое расхождение из ревью, а не улучшение
- Добавить пункт приёмки на повтор при изменившихся лимитах: ревью нашло поведение, на которое
  пункта нет
- Прогнать `./gradlew integrationTest` в окружении с Docker

## Состояние кодовой базы

- Правка лежит в рабочем дереве ветки `sdlc/PAY-412`, не закоммичена. 4 файла, +198 / −8
- **Не доделано:** валидация лимита вне идемпотентного блока — суть в
  [`verification-report-1-attempt-1.md`](verification-report-1-attempt-1.md) §2
- `common/idempotency` не тронут и трогать нельзя — механизм общий с заказами

## Отклонено и открытые вопросы

- Отклонено: своя таблица ключей для платежей — потребовала бы правки `common/idempotency`,
  а это чужой домен и сработавшее условие остановки
- Открыто на следующий виток _(дословно из «Открытых вопросов» задачи)_: добавлять ли новый
  endpoint в конфигурацию `IdempotencyCleanupJob`? Без этого строки нового endpoint'а
  не удаляются никогда и таблица растёт

## Готовность

- Пункты в `❌`: ни один · Пункты в `⚠`: claim-1, claim-2, claim-3, claim-6, claim-7, claim-8, claim-9, claim-10, claim-11
- Сборка `✅` · Тесты `✅` · Scope: файлы вне плана `✅` · Анти-обход тест-гейта `✅` ·
  Ревью независимым агентом `✅` · Секреты в diff `✅` · Интеграционные тесты `⏭`
- Закрыто вручную вместо гейта из долга: см. строки «Scope: пути плана без правок» и «Проектные
  инварианты как ассерты» в таблице «Долг» набора — даты обновлены этим витком

`⏭` у интеграционных тестов переносится **как есть**. Соблазн поставить `✅` («ну тесты же
написаны») стирает единственное наблюдение о том, что окружение не работает.

## Наблюдения витка

- Возвратов на этом chunk'е: 0 — попытка одна, приёмка была один раз и не прошла
  (из [`chunk-1-journal.md`](chunk-1-journal.md))
- Пункты, закрытые не тестом, а diff'ом: claim-4, claim-5
- Записи о дефектах на этом витке: «валидация лимита вне блока»

## Запись о проскочившем дефекте

### Запись 1

- **Имя:** валидация лимита вне блока
- **Что проскочило:** поведение повтора при изменившихся лимитах. Ревью нашло, что
  `limitValidator.check` вызывается до `idempotencyGuard.execute`; ни один пункт приёмки этого
  не покрывал
- **Какой гейт должен был поймать и почему не поймал:** пункта приёмки на это поведение не было —
  приёмочный лист был неполон; «Независимый вывод claims вторым агентом» в долге, риск принят
  и сработал
- **Действие:** проверка — добавить пункт на повтор при изменившихся условиях и тест на него
- **Кто утвердил:** А. Грицай
- **Где реализовано:** `intent.md` — новый пункт приёмки; `PaymentIdempotencyIT` — тест
  на следующей попытке

---

## Что показывает этот пример

- **Передача пишется и на обрыве, а не только на коммите.** Виток не принят, коммита нет — но
  следующая сессия начинается не с нуля: yaml-блок и «С чего начинать» дают ей состояние и первый
  шаг без чтения всей папки.
- **Запись о дефекте появилась при `retry`, не дожидаясь зелёного витка.** Кандидата назвал отчёт
  приёмки; пропуск приёмочного листа не исчезает от того, что код починят второй попыткой.
- **Статусы переносятся как есть.** `⏭` у интеграционных тестов — единственный след того, что
  в окружении нет Docker. Подмена на `✅` стирает его, и проблема окружения станет невидимой.
- **Действие выбрал человек.** Агент предложил «добавить пункт и тест»; подпись стоит рядом. Вариант
  «принять риск» закрыл бы класс дефектов, не меняя ничего в системе, — поэтому решать не агенту.
- **Приёмка — поле, не разговор.** Здесь оно заполнено «не принималась — обрыв»; на принятом витке
  тут стоят имя и дата, и следующая сессия может это проверить.
