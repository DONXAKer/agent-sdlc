# `ollama:qwen2.5-coder:7b-ctx16k` — локальная, «coder» ≠ агент

- **Задача:** фикстура `parcel-price` / `oversize`, этап 5 со снимка `oversize-plan`
  (вход побайтово тот же, что у контроля sonnet). 2026-08-29, локальная машина
  (8 ГБ VRAM), ollama, тег с `num_ctx 16384`.

## Что делалось по этапам

- **Этап 5.** 8 вызовов за 2м41с — **все чтение, ни одной записи**: `git diff` пуст,
  журнал остался бланком. 4/6 precision зелёные тривиально (проходят и без правки),
  все 3 human красные. Стоимость $0.0000 — локальный провайдер.

## Результат и вердикт

- **Этап 5: ❌** — подтверждает прежний вердикт этой же модели на другой задаче
  (там 0 вызовов за 892 с). «Кодовая» специализация — про текст кода, не про действия
  с файлами. Честный провал: ничего не сочинила, в отличие от cline_roocode:8b.

## Ресурсы

- 2м41с, бесплатно, 121 926 входных токенов.

## Файлы

- `reports/` — отчёт прогона.
