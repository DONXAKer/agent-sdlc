# Результаты прогонов моделей по методологии

Папка — свидетельства: как конкретные модели проходят виток Agent-SDLC на фикстурной
задаче, с отчётами и заполненными артефактами. Оперативный журнал «кого брать на какой
этап» живёт в раннере (`Agent-SDLC-Runner/docs/model-runs.md`) — здесь снимок результатов
с приложенными файлами, по папке на модель.

## Задача

Фикстура **`parcel-price`** (bench раннера): учебный TypeScript-проект расчёта стоимости
доставки. Основная задача — **`oversize`**: добавить в `priceFor` надбавку за негабарит
(`Quote.surcharge`): наибольшая сторона >120 см → 40%, сумма трёх измерений >300 см → 90%
(ставка 90% приходит **только из ответа человека** на этапе вопросов — так меряется
удержание факта из диалога), правила не складываются — применяется бо́льшая; правило —
отдельным модулем `src/oversize.ts`.

Оценка — **9 скрытых тестов** (модель их не видит): 2 regression + 4 precision (точность
границ 120/300 см) + 3 human (дошла ли ставка 90% из ответа человека до кода). Плюс щупы
честности: заполнен ли артефакт, совпал ли diff с деревом, не сочинён ли отчёт о тестах.

## Сводка: этап 5 (chunk) со снимка `oversize-plan`, один и тот же вход

| Модель | ~ГБ Q4 | Скрытые тесты | Этап закрыт | До вердикта эт. 6 | Время | Цена | Вердикт |
|---|---|---|---|---|---|---|---|
| `claude-sdk:sonnet` (контроль, `--all`) | облако | **9/9** | ✅ | SDK-баг #376 | ~10 мин виток | $3.57 виток | ✅ эталон |
| `polza:ministral-14b` | 9 | **9/9** | ✅ дозап. | ✅ | 2м45с | ₽2.7 | ✅ с `formFill` |
| `polza:seed-2.0-mini` | ? | **9/9** | ✅ дозап. | ✅ | 4м50с | ₽12.9 | ✅ по содержанию, дорога |
| `polza:ministral-8b` | 5 | 6/6 + 1/3 human | ✅ | ✅ | 5–25 мин | ₽2.6–4.0 | ⚠ лучший ≤8 ГБ |
| `polza:qwen3-8b` | 5 | 4/6 + 0/3 | ✅ сама | ✅ | 7м02с | ₽5.5 | ⚠ оформление без качества |
| `claude-sdk:haiku` (контроль) | облако | 4/6 + 0/3 | ❌ журнал | — | 2м35с | $0.26 | порог журнала общий |
| `polza:qwen3-14b` | 9 | 4/6 + 0/3 | ✅ сама | ❌ | 4м10с | ₽8.6 | ⚠ опасна, не брать |
| `polza:gpt-oss-20b` | 12 | 4/6 + 0/3 | ❌ бланк | — | 3м51с | ₽0.5 | ❌ эт. 5 |
| `polza:qwen3.5-9b` | 6 | 4/6 + 0/3 | ❌ дерево пусто | — | 1м22с | ₽0.3 | ❌ «читает и не пишет» |
| `polza:qwen-2.5-7b` | 5 | 4/6 + 0/3 | ❌ дерево пусто | — | 0м27с | ₽0.8 | ❌ |
| `ollama:qwen2.5-coder:7b` | 5 | 4/6 + 0/3 | ❌ | — | 2м41с | $0 | ❌ «coder» ≠ агент |

(4/6 — тривиально зелёные кейсы, проходящие и без правки; «дозап.» — этап закрыт
дозаполнением журнала по полям.)

## Рецензент этапа 6 (со снимка `oversize-chunk`)

| Кандидат | Итог | Отчёт vs факт гейтов | Цена |
|---|---|---|---|
| `polza:ministral-14b` (лимит 60 ходов) | ✅ рецензент найден | 0 расхождений, находка ревью записана | ₽5.9 |
| `polza:gpt-oss-20b` | «оформитель», не ревью | 4 расхождения | ₽0.48 |
| `polza:qwen3-14b` | ❌ залипла на Read | — | ₽23.5 |

## Общий вердикт

- **Класс ≤16 ГБ доводит виток до вердикта**: исполнитель `ministral-14b` (9/9, ₽2.7,
  2м45с) + он же рецензентом при лимите 60 ходов. Правило «рецензент строго сильнее
  исполнителя» собирается целиком без облачных моделей: chunk `ministral-8b` (rank 30) →
  verify `ministral-14b` (rank 40).
- **Класс ≤8 ГБ**: `ministral-8b` (5 ГБ) стабильно пишет корректный код границ и доводит
  виток до вердикта; систематически слаб в одном — донести ответ человека до кода (1/3).
  Против этого в раннер добавлены карточка фактов в промпт и гейт «Ответы человека в коде».
- **Пороги — не в размере модели**: контрольный haiku упал на том же пороге журнала, что
  и локальные; qwen3.5-9b «не звавшая инструменты» локально оказалась порогом среды, а не
  модели. Среда — часть настройки; свидетельства производит рантайм, не модель.
- **Суммарно потрачено на все серии r2–r9**: порядка ₽120 через polza + ~$4.5 подписочных
  контролей; календарно — один день замеров (2026-08-30) плюс контрольный день (08-29).

## Структура

В каждой папке: `README.md` (задача, что делалось по этапам, результат, ресурсы, вердикт),
`reports/` (машинные отчёты bench как есть), `artifacts/` (заполненные артефакты витка —
там, где рабочая копия дожила до снимка; у остальных артефакты пересказаны в отчётах).
